README.md
CLAUDE.md
docs/
scripts/

# Rectangle config (handled by run_once script, not direct symlink)
.config/rectangle/**

{{- if eq .chezmoi.hostname "A6436902" }}
Source/*
!Source/Jeppesen
{{- else }}
Source/Jeppesen
{{- end }}

# Base content for the .claude/settings.json modify_ script; not deployed itself.
.claude/settings.base.json

# Generated by aws-sso (BEGIN_AWS_SSO_CLI markers, AutoConfigCheck: true).
# It rewrites these whenever account access changes, so chezmoi cannot own them.
# Run `aws-sso config` on a new machine to regenerate.
Source/CustomerProjects/Intersolia/.aws/config
Source/CustomerProjects/Intersolia/.aws-sso/config
